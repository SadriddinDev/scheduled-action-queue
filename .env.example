# Docker Compose (default)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/taskqueue

# Local PostgreSQL without password (replace YOUR_USER with your OS username)
# DATABASE_URL=postgresql+asyncpg://YOUR_USER@localhost:5432/taskqueue
